<%= form_with(model: damagebook, local: true) do |form| %>
  <% if damagebook.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(damagebook.errors.count, "error") %> prohibited this damagebook from being saved:</h2>

      <ul>
        <% damagebook.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <strong>Bookid:</strong>
    <%= params[:book_id] %>
  </p>

  <p>
    <strong>Booktitle:</strong>
    <%= params[:book_title] %>
  </p>

  <p>
    <strong>Borrowerid:</strong>
    <%= params[:borrower_id] %>
  </p>

  <p>
    <strong>Damagedate:</strong>
    <%= Date.today %>
  </p>

  <%= hidden_field_tag :book_id, params[:book_id] %>

  <%= hidden_field_tag :book_title, params[:book_title] %>

  <%= hidden_field_tag :borrower_id, params[:borrower_id] %>

  <%= hidden_field_tag :damage_date, Date.today %>


  <div class="field">
    <%= form.label :level %>
    <%= form.text_field :level %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
