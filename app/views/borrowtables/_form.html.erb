<%= form_with(model: borrowtable,book_id:@book_id,book_title:@book_title, local: true) do |form| %>
  <% if borrowtable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(borrowtable.errors.count, "error") %> prohibited this borrowtable from being saved:</h2>

      <ul>
        <% borrowtable.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <strong>图书编号:</strong>
    <%= params[:book_id] %>
  </p>

  <p>
    <strong>书名:</strong>
    <%= params[:book_title] %>
  </p>

  <p>
    <strong>借阅时间:</strong>
    <%= Date.today %>
  </p>

  <p>
    <strong>借阅日期:</strong>
    <%= Date.today + 30.day %>
  </p>

  <p>
    <strong>借阅人学号:</strong>
    <%= current_user.userid %>
  </p>

  <%= hidden_field_tag :book_id, params[:book_id] %>

  <%= hidden_field_tag :book_title, params[:book_title] %>

  <%= hidden_field_tag :borrow_date, Date.today %>

  <%= hidden_field_tag :return_date, Date.today + 30.day %>

  <%= hidden_field_tag :borrowerid, current_user.userid %>

  <div class="actions">
    <%= form.submit "确认"%>
  </div>
<% end %>
