<%= form_with(model: student, local: true) do |form| %>
  <% if student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student.errors.count, "error") %> prohibited this student from being saved:</h2>

      <ul>
        <% student.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :id %>
    <%= form.text_field :id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :gender %>
    <%= form.collection_select :gender,Student.genders.map { |a|
      [a.first,a.first]}, :first, :second%>
  </div>

  <div class="field">
    <%= form.label :department %>
    <%= form.text_field :department %>
  </div>

  <div class="field">
    <%= form.label :registration %>
    <%= form.date_select :registration %>
  </div>

  <div class="field">
    <%= form.label :validity %>
    <%= form.date_select :validity %>
  </div>

  <div class="field">
    <%= form.label :credit %>
    <%= form.number_field :credit %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
