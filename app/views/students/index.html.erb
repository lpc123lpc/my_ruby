<p id="notice"><%= notice %></p>

<h1>学生信息</h1>

<table>
  <thead>
    <tr>
      <th>学号</th>
      <th>姓名</th>
      <th>性别</th>
      <th>系号</th>
      <th>注册日期</th>
      <th>有效期</th>
      <th>信用分</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @students.each do |student| %>
      <tr>
        <td><%= student.id %></td>
        <td><%= student.name %></td>
        <td><%= student.gender %></td>
        <td><%= student.department %></td>
        <td><%= student.registration %></td>
        <td><%= student.validity %></td>
        <td><%= student.credit %></td>
        <td><%= link_to '详细信息', student %></td>
        <td><%= link_to '编辑学生信息', edit_student_path(student) %></td>
        <td><%= link_to '删除学生', student, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '新增学生', new_student_path %>
